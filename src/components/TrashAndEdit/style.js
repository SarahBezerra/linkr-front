import styled from "styled-components";

const BoxIcons = styled.div`
  margin-top: 5px;
  position: absolute;
  top: 0;
  right: 0;

  span {
    margin-left: 5px;
    :hover {
      cursor: pointer;
    }
  }
`;

export default BoxIcons;
